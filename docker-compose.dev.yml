version: '3'
services:
  game-upload:
    command: "yarn run dev"
    volumes: 
      - ./private:/app/private
      - ./public:/app/public
      - ./src:/app/src
      - ./views:/app/views
    env_file:
      - docker-dev.env
